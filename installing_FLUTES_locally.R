install.packages("", # specify file path to package folder   
                 repos = NULL, # Can be NULL to install from local files
                 dependencies = T,
                 type = "source")